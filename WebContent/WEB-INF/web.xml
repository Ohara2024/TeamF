<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">

    <!-- アプリケーションのルート設定 -->
    <display-name>TeamF</display-name>

    <!-- サーブレットの設定 -->
    <servlet>
        <servlet-name>StudentListAction</servlet-name>
        <servlet-class>servlet.StudentListAction</servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>StudentListAction</servlet-name>
        <url-pattern>/StudentListAction</url-pattern>
    </servlet-mapping>

    <!-- その他サーブレットを追加する場合の例
    <servlet>
        <servlet-name>AnotherServlet</servlet-name>
        <servlet-class>servlet.AnotherServlet</servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>AnotherServlet</servlet-name>
        <url-pattern>/AnotherServlet</url-pattern>
    </servlet-mapping>
    -->

    <!-- 文字エンコーディングフィルター（任意） -->
    <filter>
        <filter-name>EncodingFilter</filter-name>
        <filter-class>org.apache.catalina.filters.SetCharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>ignore</param-name>
            <param-value>false</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>EncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- ウェルカムページ -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

</web-app>
